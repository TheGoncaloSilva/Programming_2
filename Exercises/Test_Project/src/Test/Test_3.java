package Test;

//Change whatever you like.

/*
	import java.awt.Canvas;
	import java.awt.Graphics;
	//import java.awt.Dimension;
	import javax.swing.*;*/

public class Test_3 {
//public class Test_3 extends Canvas {
	   /*
	   private static int UNIT = 128;

	   public void paint(Graphics g) {
	      mickeyFractal(g, 2*UNIT, 2*UNIT, UNIT, 2);
	      mickeyFractal(g, 6*UNIT, 2*UNIT, UNIT, 6);
	   }
	   
	   // Draw a filled circle with center (x, y) and radius r.
	   public void circle(Graphics g, int x, int y, int r) {
	      g.fillOval(x-r, y-r, 2*r, 2*r);
	   }

	   // Draw a Mickey fractal with n levels.
	   // n=1 produces a circle, n=2 produces a familiar face.
	   public void mickeyFractal(Graphics g, int x, int y, int r, int n) {
	      // The largest circle has center (x, y) and radius r.
	      circle(g, x, y, r);
	      //...
	   }

	   // Test the class
	   public static void main(String[] args) {
	      JFrame frame = new JFrame();
	      frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
	      frame.setSize(8*UNIT, 3*UNIT);
	      Canvas canvas = new TestDrawing();
	      canvas.setSize(8*UNIT, 3*UNIT);
	    
	      frame.add(canvas);
	      frame.setVisible(true);
	   }*/
	}